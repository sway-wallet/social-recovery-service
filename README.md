# social-recovery-service

To install dependencies:

```bash
bun install
```

To run with docker compose:

```bash
bun docker
```

To clean up docker compose:

```bash
bun docker:clean
```


To clean bun:
```bash
bun clean
```

